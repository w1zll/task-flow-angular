/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkspaceMemberResponseDto } from '../../models/workspace-member-response-dto';

export interface WorkspacesControllerMembers$Params {
  id: string;
}

export function workspacesControllerMembers(http: HttpClient, rootUrl: string, params: WorkspacesControllerMembers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<WorkspaceMemberResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, workspacesControllerMembers.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<WorkspaceMemberResponseDto>>;
    })
  );
}

workspacesControllerMembers.PATH = '/api/workspaces/{id}/members';
