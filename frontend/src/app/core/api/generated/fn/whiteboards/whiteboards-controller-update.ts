/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UpdateWhiteboardDto } from '../../models/update-whiteboard-dto';
import { WhiteboardResponseDto } from '../../models/whiteboard-response-dto';

export interface WhiteboardsControllerUpdate$Params {
  workspaceId: string;
  whiteboardId: string;
      body: UpdateWhiteboardDto
}

export function whiteboardsControllerUpdate(http: HttpClient, rootUrl: string, params: WhiteboardsControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<WhiteboardResponseDto>> {
  const rb = new RequestBuilder(rootUrl, whiteboardsControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.path('whiteboardId', params.whiteboardId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WhiteboardResponseDto>;
    })
  );
}

whiteboardsControllerUpdate.PATH = '/api/workspaces/{workspaceId}/whiteboards/{whiteboardId}';
