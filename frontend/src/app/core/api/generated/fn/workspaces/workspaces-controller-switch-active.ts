/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkspaceResponseDto } from '../../models/workspace-response-dto';

export interface WorkspacesControllerSwitchActive$Params {
  id: string;
}

export function workspacesControllerSwitchActive(http: HttpClient, rootUrl: string, params: WorkspacesControllerSwitchActive$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkspaceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, workspacesControllerSwitchActive.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkspaceResponseDto>;
    })
  );
}

workspacesControllerSwitchActive.PATH = '/api/workspaces/{id}/active';
