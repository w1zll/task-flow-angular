/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReorderTaskChecklistItemsDto } from '../../models/reorder-task-checklist-items-dto';
import { TaskChecklistItemResponseDto } from '../../models/task-checklist-item-response-dto';

export interface TasksControllerReorderChecklistItems$Params {
  taskId: string;
      body: ReorderTaskChecklistItemsDto
}

export function tasksControllerReorderChecklistItems(http: HttpClient, rootUrl: string, params: TasksControllerReorderChecklistItems$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TaskChecklistItemResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, tasksControllerReorderChecklistItems.PATH, 'put');
  if (params) {
    rb.path('taskId', params.taskId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TaskChecklistItemResponseDto>>;
    })
  );
}

tasksControllerReorderChecklistItems.PATH = '/api/tasks/{taskId}/checklist-items/reorder';
