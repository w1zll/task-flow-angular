/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReorderColumnsDto } from '../../models/reorder-columns-dto';

export interface ColumnsControllerReorder$Params {
  boardId: string;
      body: ReorderColumnsDto
}

export function columnsControllerReorder(http: HttpClient, rootUrl: string, params: ColumnsControllerReorder$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, columnsControllerReorder.PATH, 'put');
  if (params) {
    rb.path('boardId', params.boardId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

columnsControllerReorder.PATH = '/api/columns/board/{boardId}/reorder';
