/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface TasksControllerAttachmentFile$Params {
  taskId: string;
  attachmentId: string;
}

export function tasksControllerAttachmentFile(http: HttpClient, rootUrl: string, params: TasksControllerAttachmentFile$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
  const rb = new RequestBuilder(rootUrl, tasksControllerAttachmentFile.PATH, 'get');
  if (params) {
    rb.path('taskId', params.taskId, {});
    rb.path('attachmentId', params.attachmentId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Blob>;
    })
  );
}

tasksControllerAttachmentFile.PATH = '/api/tasks/{taskId}/attachments/{attachmentId}/file';
