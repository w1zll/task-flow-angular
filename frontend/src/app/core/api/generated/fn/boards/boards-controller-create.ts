/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardResponseDto } from '../../models/board-response-dto';
import { CreateBoardDto } from '../../models/create-board-dto';

export interface BoardsControllerCreate$Params {
      body: CreateBoardDto
}

export function boardsControllerCreate(http: HttpClient, rootUrl: string, params: BoardsControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<BoardResponseDto>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BoardResponseDto>;
    })
  );
}

boardsControllerCreate.PATH = '/api/boards';
