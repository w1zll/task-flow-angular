/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AnalyticsItemDto } from '../../models/analytics-item-dto';

export interface TasksControllerAnalyticsDaily$Params {
  boardId?: string;
  fromDate?: string;
  toDate?: string;
}

export function tasksControllerAnalyticsDaily(http: HttpClient, rootUrl: string, params?: TasksControllerAnalyticsDaily$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<AnalyticsItemDto>>> {
  const rb = new RequestBuilder(rootUrl, tasksControllerAnalyticsDaily.PATH, 'get');
  if (params) {
    rb.query('boardId', params.boardId, {});
    rb.query('fromDate', params.fromDate, {});
    rb.query('toDate', params.toDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<AnalyticsItemDto>>;
    })
  );
}

tasksControllerAnalyticsDaily.PATH = '/api/tasks/analytics/daily';
