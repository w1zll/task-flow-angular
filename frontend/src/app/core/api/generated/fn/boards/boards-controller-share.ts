/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardMemberResponseDto } from '../../models/board-member-response-dto';
import { ShareBoardDto } from '../../models/share-board-dto';

export interface BoardsControllerShare$Params {
  id: string;
      body: ShareBoardDto
}

export function boardsControllerShare(http: HttpClient, rootUrl: string, params: BoardsControllerShare$Params, context?: HttpContext): Observable<StrictHttpResponse<BoardMemberResponseDto>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerShare.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BoardMemberResponseDto>;
    })
  );
}

boardsControllerShare.PATH = '/api/boards/{id}/share';
