/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AnalyticsDashboardResponseDto } from '../../models/analytics-dashboard-response-dto';

export interface AnalyticsControllerDashboard$Params {
  workspaceId: string;
  boardId?: string;
  teamId?: string;
  assigneeId?: string;
  fromDate?: string;
  toDate?: string;
}

export function analyticsControllerDashboard(http: HttpClient, rootUrl: string, params: AnalyticsControllerDashboard$Params, context?: HttpContext): Observable<StrictHttpResponse<AnalyticsDashboardResponseDto>> {
  const rb = new RequestBuilder(rootUrl, analyticsControllerDashboard.PATH, 'get');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.query('boardId', params.boardId, {});
    rb.query('teamId', params.teamId, {});
    rb.query('assigneeId', params.assigneeId, {});
    rb.query('fromDate', params.fromDate, {});
    rb.query('toDate', params.toDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AnalyticsDashboardResponseDto>;
    })
  );
}

analyticsControllerDashboard.PATH = '/api/workspaces/{workspaceId}/analytics/dashboard';
