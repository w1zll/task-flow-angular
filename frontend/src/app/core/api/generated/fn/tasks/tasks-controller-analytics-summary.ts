/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CompletionSummaryDto } from '../../models/completion-summary-dto';

export interface TasksControllerAnalyticsSummary$Params {
  boardId?: string;
  fromDate?: string;
  toDate?: string;
}

export function tasksControllerAnalyticsSummary(http: HttpClient, rootUrl: string, params?: TasksControllerAnalyticsSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<CompletionSummaryDto>> {
  const rb = new RequestBuilder(rootUrl, tasksControllerAnalyticsSummary.PATH, 'get');
  if (params) {
    rb.query('boardId', params.boardId, {});
    rb.query('fromDate', params.fromDate, {});
    rb.query('toDate', params.toDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CompletionSummaryDto>;
    })
  );
}

tasksControllerAnalyticsSummary.PATH = '/api/tasks/analytics/summary';
