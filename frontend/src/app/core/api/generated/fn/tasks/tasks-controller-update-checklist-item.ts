/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TaskChecklistItemResponseDto } from '../../models/task-checklist-item-response-dto';
import { UpdateTaskChecklistItemDto } from '../../models/update-task-checklist-item-dto';

export interface TasksControllerUpdateChecklistItem$Params {
  taskId: string;
  itemId: string;
      body: UpdateTaskChecklistItemDto
}

export function tasksControllerUpdateChecklistItem(http: HttpClient, rootUrl: string, params: TasksControllerUpdateChecklistItem$Params, context?: HttpContext): Observable<StrictHttpResponse<TaskChecklistItemResponseDto>> {
  const rb = new RequestBuilder(rootUrl, tasksControllerUpdateChecklistItem.PATH, 'put');
  if (params) {
    rb.path('taskId', params.taskId, {});
    rb.path('itemId', params.itemId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TaskChecklistItemResponseDto>;
    })
  );
}

tasksControllerUpdateChecklistItem.PATH = '/api/tasks/{taskId}/checklist-items/{itemId}';
