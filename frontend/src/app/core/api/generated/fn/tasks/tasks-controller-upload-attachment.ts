/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TaskAttachmentResponseDto } from '../../models/task-attachment-response-dto';
import { UploadTaskAttachmentDto } from '../../models/upload-task-attachment-dto';

export interface TasksControllerUploadAttachment$Params {
  taskId: string;
      body: UploadTaskAttachmentDto
}

export function tasksControllerUploadAttachment(http: HttpClient, rootUrl: string, params: TasksControllerUploadAttachment$Params, context?: HttpContext): Observable<StrictHttpResponse<TaskAttachmentResponseDto>> {
  const rb = new RequestBuilder(rootUrl, tasksControllerUploadAttachment.PATH, 'post');
  if (params) {
    rb.path('taskId', params.taskId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TaskAttachmentResponseDto>;
    })
  );
}

tasksControllerUploadAttachment.PATH = '/api/tasks/{taskId}/attachments';
