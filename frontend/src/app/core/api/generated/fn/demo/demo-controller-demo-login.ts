/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DemoWorkspaceSessionDto } from '../../models/demo-workspace-session-dto';

export interface DemoControllerDemoLogin$Params {
}

export function demoControllerDemoLogin(http: HttpClient, rootUrl: string, params?: DemoControllerDemoLogin$Params, context?: HttpContext): Observable<StrictHttpResponse<DemoWorkspaceSessionDto>> {
  const rb = new RequestBuilder(rootUrl, demoControllerDemoLogin.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DemoWorkspaceSessionDto>;
    })
  );
}

demoControllerDemoLogin.PATH = '/api/auth/demo-login';
