/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ColumnResponseDto } from '../../models/column-response-dto';
import { UpdateColumnDto } from '../../models/update-column-dto';

export interface ColumnsControllerUpdate$Params {
  id: string;
      body: UpdateColumnDto
}

export function columnsControllerUpdate(http: HttpClient, rootUrl: string, params: ColumnsControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<ColumnResponseDto>> {
  const rb = new RequestBuilder(rootUrl, columnsControllerUpdate.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ColumnResponseDto>;
    })
  );
}

columnsControllerUpdate.PATH = '/api/columns/{id}';
