/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WhiteboardResponseDto } from '../../models/whiteboard-response-dto';

export interface WhiteboardsControllerList$Params {
  workspaceId: string;
  boardId?: string;
}

export function whiteboardsControllerList(http: HttpClient, rootUrl: string, params: WhiteboardsControllerList$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<WhiteboardResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, whiteboardsControllerList.PATH, 'get');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.query('boardId', params.boardId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<WhiteboardResponseDto>>;
    })
  );
}

whiteboardsControllerList.PATH = '/api/workspaces/{workspaceId}/whiteboards';
