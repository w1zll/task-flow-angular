/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardMemberResponseDto } from '../../models/board-member-response-dto';

export interface BoardsControllerMembers$Params {
  id: string;
}

export function boardsControllerMembers(http: HttpClient, rootUrl: string, params: BoardsControllerMembers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<BoardMemberResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerMembers.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<BoardMemberResponseDto>>;
    })
  );
}

boardsControllerMembers.PATH = '/api/boards/{id}/members';
