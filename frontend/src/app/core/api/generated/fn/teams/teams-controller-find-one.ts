/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TeamResponseDto } from '../../models/team-response-dto';

export interface TeamsControllerFindOne$Params {
  workspaceId: string;
  teamId: string;
}

export function teamsControllerFindOne(http: HttpClient, rootUrl: string, params: TeamsControllerFindOne$Params, context?: HttpContext): Observable<StrictHttpResponse<TeamResponseDto>> {
  const rb = new RequestBuilder(rootUrl, teamsControllerFindOne.PATH, 'get');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.path('teamId', params.teamId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TeamResponseDto>;
    })
  );
}

teamsControllerFindOne.PATH = '/api/workspaces/{workspaceId}/teams/{teamId}';
