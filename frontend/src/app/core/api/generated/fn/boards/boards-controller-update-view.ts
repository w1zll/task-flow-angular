/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardViewResponseDto } from '../../models/board-view-response-dto';
import { UpdateBoardViewDto } from '../../models/update-board-view-dto';

export interface BoardsControllerUpdateView$Params {
  id: string;
  viewId: string;
      body: UpdateBoardViewDto
}

export function boardsControllerUpdateView(http: HttpClient, rootUrl: string, params: BoardsControllerUpdateView$Params, context?: HttpContext): Observable<StrictHttpResponse<BoardViewResponseDto>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerUpdateView.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('viewId', params.viewId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BoardViewResponseDto>;
    })
  );
}

boardsControllerUpdateView.PATH = '/api/boards/{id}/views/{viewId}';
