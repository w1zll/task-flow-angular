/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardViewResponseDto } from '../../models/board-view-response-dto';
import { CreateBoardViewDto } from '../../models/create-board-view-dto';

export interface BoardsControllerCreateView$Params {
  id: string;
      body: CreateBoardViewDto
}

export function boardsControllerCreateView(http: HttpClient, rootUrl: string, params: BoardsControllerCreateView$Params, context?: HttpContext): Observable<StrictHttpResponse<BoardViewResponseDto>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerCreateView.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BoardViewResponseDto>;
    })
  );
}

boardsControllerCreateView.PATH = '/api/boards/{id}/views';
