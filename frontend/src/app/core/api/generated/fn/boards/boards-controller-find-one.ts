/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardResponseDto } from '../../models/board-response-dto';

export interface BoardsControllerFindOne$Params {
  id: string;
}

export function boardsControllerFindOne(http: HttpClient, rootUrl: string, params: BoardsControllerFindOne$Params, context?: HttpContext): Observable<StrictHttpResponse<BoardResponseDto>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerFindOne.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BoardResponseDto>;
    })
  );
}

boardsControllerFindOne.PATH = '/api/boards/{id}';
