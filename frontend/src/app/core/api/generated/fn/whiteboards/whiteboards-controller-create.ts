/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateWhiteboardDto } from '../../models/create-whiteboard-dto';
import { WhiteboardResponseDto } from '../../models/whiteboard-response-dto';

export interface WhiteboardsControllerCreate$Params {
  workspaceId: string;
      body: CreateWhiteboardDto
}

export function whiteboardsControllerCreate(http: HttpClient, rootUrl: string, params: WhiteboardsControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<WhiteboardResponseDto>> {
  const rb = new RequestBuilder(rootUrl, whiteboardsControllerCreate.PATH, 'post');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WhiteboardResponseDto>;
    })
  );
}

whiteboardsControllerCreate.PATH = '/api/workspaces/{workspaceId}/whiteboards';
