/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardViewResponseDto } from '../../models/board-view-response-dto';

export interface BoardsControllerViews$Params {
  id: string;
}

export function boardsControllerViews(http: HttpClient, rootUrl: string, params: BoardsControllerViews$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<BoardViewResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerViews.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<BoardViewResponseDto>>;
    })
  );
}

boardsControllerViews.PATH = '/api/boards/{id}/views';
