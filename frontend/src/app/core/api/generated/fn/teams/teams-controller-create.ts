/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateTeamDto } from '../../models/create-team-dto';
import { TeamResponseDto } from '../../models/team-response-dto';

export interface TeamsControllerCreate$Params {
  workspaceId: string;
      body: CreateTeamDto
}

export function teamsControllerCreate(http: HttpClient, rootUrl: string, params: TeamsControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<TeamResponseDto>> {
  const rb = new RequestBuilder(rootUrl, teamsControllerCreate.PATH, 'post');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TeamResponseDto>;
    })
  );
}

teamsControllerCreate.PATH = '/api/workspaces/{workspaceId}/teams';
