/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SessionDto } from '../../models/session-dto';

export interface AuthControllerSessions$Params {
}

export function authControllerSessions(http: HttpClient, rootUrl: string, params?: AuthControllerSessions$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<SessionDto>>> {
  const rb = new RequestBuilder(rootUrl, authControllerSessions.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<SessionDto>>;
    })
  );
}

authControllerSessions.PATH = '/api/auth/sessions';
