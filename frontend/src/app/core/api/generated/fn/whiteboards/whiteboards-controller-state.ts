/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WhiteboardStateResponseDto } from '../../models/whiteboard-state-response-dto';

export interface WhiteboardsControllerState$Params {
  workspaceId: string;
  whiteboardId: string;
  afterSequence?: string;
}

export function whiteboardsControllerState(http: HttpClient, rootUrl: string, params: WhiteboardsControllerState$Params, context?: HttpContext): Observable<StrictHttpResponse<WhiteboardStateResponseDto>> {
  const rb = new RequestBuilder(rootUrl, whiteboardsControllerState.PATH, 'get');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.path('whiteboardId', params.whiteboardId, {});
    rb.query('afterSequence', params.afterSequence, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WhiteboardStateResponseDto>;
    })
  );
}

whiteboardsControllerState.PATH = '/api/workspaces/{workspaceId}/whiteboards/{whiteboardId}/state';
