/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TeamResponseDto } from '../../models/team-response-dto';

export interface TeamsControllerListMine$Params {
  workspaceId: string;
}

export function teamsControllerListMine(http: HttpClient, rootUrl: string, params: TeamsControllerListMine$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TeamResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, teamsControllerListMine.PATH, 'get');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TeamResponseDto>>;
    })
  );
}

teamsControllerListMine.PATH = '/api/workspaces/{workspaceId}/teams/mine';
