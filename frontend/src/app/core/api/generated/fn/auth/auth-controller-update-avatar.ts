/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AvatarUploadDto } from '../../models/avatar-upload-dto';
import { UserDto } from '../../models/user-dto';

export interface AuthControllerUpdateAvatar$Params {
      body: AvatarUploadDto
}

export function authControllerUpdateAvatar(http: HttpClient, rootUrl: string, params: AuthControllerUpdateAvatar$Params, context?: HttpContext): Observable<StrictHttpResponse<UserDto>> {
  const rb = new RequestBuilder(rootUrl, authControllerUpdateAvatar.PATH, 'put');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UserDto>;
    })
  );
}

authControllerUpdateAvatar.PATH = '/api/auth/avatar';
