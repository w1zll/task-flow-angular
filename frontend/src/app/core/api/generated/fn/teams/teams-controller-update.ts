/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TeamResponseDto } from '../../models/team-response-dto';
import { UpdateTeamDto } from '../../models/update-team-dto';

export interface TeamsControllerUpdate$Params {
  workspaceId: string;
  teamId: string;
      body: UpdateTeamDto
}

export function teamsControllerUpdate(http: HttpClient, rootUrl: string, params: TeamsControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<TeamResponseDto>> {
  const rb = new RequestBuilder(rootUrl, teamsControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.path('teamId', params.teamId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TeamResponseDto>;
    })
  );
}

teamsControllerUpdate.PATH = '/api/workspaces/{workspaceId}/teams/{teamId}';
