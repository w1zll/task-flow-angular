/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MoveTaskDto } from '../../models/move-task-dto';
import { TaskResponseDto } from '../../models/task-response-dto';

export interface TasksControllerMove$Params {
  id: string;
      body: MoveTaskDto
}

export function tasksControllerMove(http: HttpClient, rootUrl: string, params: TasksControllerMove$Params, context?: HttpContext): Observable<StrictHttpResponse<TaskResponseDto>> {
  const rb = new RequestBuilder(rootUrl, tasksControllerMove.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TaskResponseDto>;
    })
  );
}

tasksControllerMove.PATH = '/api/tasks/{id}/move';
