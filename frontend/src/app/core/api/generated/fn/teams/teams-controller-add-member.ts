/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AddTeamMemberDto } from '../../models/add-team-member-dto';
import { TeamMemberResponseDto } from '../../models/team-member-response-dto';

export interface TeamsControllerAddMember$Params {
  workspaceId: string;
  teamId: string;
      body: AddTeamMemberDto
}

export function teamsControllerAddMember(http: HttpClient, rootUrl: string, params: TeamsControllerAddMember$Params, context?: HttpContext): Observable<StrictHttpResponse<TeamMemberResponseDto>> {
  const rb = new RequestBuilder(rootUrl, teamsControllerAddMember.PATH, 'post');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.path('teamId', params.teamId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TeamMemberResponseDto>;
    })
  );
}

teamsControllerAddMember.PATH = '/api/workspaces/{workspaceId}/teams/{teamId}/members';
