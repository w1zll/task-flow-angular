/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TeamMemberResponseDto } from '../../models/team-member-response-dto';

export interface TeamsControllerListMembers$Params {
  workspaceId: string;
  teamId: string;
}

export function teamsControllerListMembers(http: HttpClient, rootUrl: string, params: TeamsControllerListMembers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TeamMemberResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, teamsControllerListMembers.PATH, 'get');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.path('teamId', params.teamId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TeamMemberResponseDto>>;
    })
  );
}

teamsControllerListMembers.PATH = '/api/workspaces/{workspaceId}/teams/{teamId}/members';
