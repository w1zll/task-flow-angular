/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateWorkspaceDto } from '../../models/create-workspace-dto';
import { WorkspaceResponseDto } from '../../models/workspace-response-dto';

export interface WorkspacesControllerCreate$Params {
      body: CreateWorkspaceDto
}

export function workspacesControllerCreate(http: HttpClient, rootUrl: string, params: WorkspacesControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkspaceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, workspacesControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkspaceResponseDto>;
    })
  );
}

workspacesControllerCreate.PATH = '/api/workspaces';
