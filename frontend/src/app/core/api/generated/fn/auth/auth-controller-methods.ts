/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AuthMethodsDto } from '../../models/auth-methods-dto';

export interface AuthControllerMethods$Params {
}

export function authControllerMethods(http: HttpClient, rootUrl: string, params?: AuthControllerMethods$Params, context?: HttpContext): Observable<StrictHttpResponse<AuthMethodsDto>> {
  const rb = new RequestBuilder(rootUrl, authControllerMethods.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AuthMethodsDto>;
    })
  );
}

authControllerMethods.PATH = '/api/auth/methods';
