/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface TeamsControllerRemoveMember$Params {
  workspaceId: string;
  teamId: string;
  memberId: string;
}

export function teamsControllerRemoveMember(http: HttpClient, rootUrl: string, params: TeamsControllerRemoveMember$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, teamsControllerRemoveMember.PATH, 'delete');
  if (params) {
    rb.path('workspaceId', params.workspaceId, {});
    rb.path('teamId', params.teamId, {});
    rb.path('memberId', params.memberId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

teamsControllerRemoveMember.PATH = '/api/workspaces/{workspaceId}/teams/{teamId}/members/{memberId}';
