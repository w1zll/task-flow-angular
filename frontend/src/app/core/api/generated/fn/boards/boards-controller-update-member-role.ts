/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BoardMemberResponseDto } from '../../models/board-member-response-dto';
import { UpdateBoardMemberRoleDto } from '../../models/update-board-member-role-dto';

export interface BoardsControllerUpdateMemberRole$Params {
  id: string;
  memberId: string;
      body: UpdateBoardMemberRoleDto
}

export function boardsControllerUpdateMemberRole(http: HttpClient, rootUrl: string, params: BoardsControllerUpdateMemberRole$Params, context?: HttpContext): Observable<StrictHttpResponse<BoardMemberResponseDto>> {
  const rb = new RequestBuilder(rootUrl, boardsControllerUpdateMemberRole.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('memberId', params.memberId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BoardMemberResponseDto>;
    })
  );
}

boardsControllerUpdateMemberRole.PATH = '/api/boards/{id}/members/{memberId}/role';
